const KEY='note-architect-v5';
const state=load();
bindTabs();bindCRUD();bindAI();bindData();renderAll();
function base(){return{chapters:[],scenes:[],characters:[],worldEntries:[],timelineEvents:[]}};
function load(){try{return{...base(),...JSON.parse(localStorage.getItem(KEY)||'{}')}}catch{return base();}}
function save(){localStorage.setItem(KEY,JSON.stringify(state));}
const id=()=>crypto.randomUUID();const v=(x)=>document.getElementById(x).value.trim();
function esc(s){return String(s).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#39;');}
function bindTabs(){document.querySelectorAll('.tab-btn').forEach(b=>b.onclick=()=>{document.querySelectorAll('.tab-btn,.tab').forEach(x=>x.classList.remove('active'));b.classList.add('active');document.getElementById(`tab-${b.dataset.tab}`).classList.add('active');});}
function bindCRUD(){
  document.getElementById('save-chapter').onclick=()=>add('chapters',{title:v('chapter-title')},['chapter-title'],()=>{renderChapters();renderSceneChapterSelect();});
  document.getElementById('save-scene').onclick=()=>add('scenes',{title:v('scene-title'),chapterId:document.getElementById('scene-chapter').value,content:v('scene-content')},['scene-title','scene-content'],renderScenes, x=>x.title&&x.content);
  document.getElementById('save-character').onclick=()=>add('characters',{name:v('character-name'),role:v('character-role'),note:v('character-note')},['character-name','character-role','character-note'],renderCharacters,x=>x.name);
  document.getElementById('save-world').onclick=()=>add('worldEntries',{type:document.getElementById('world-type').value,name:v('world-name'),description:v('world-description')},['world-name','world-description'],renderWorld,x=>x.name);
  document.getElementById('save-timeline').onclick=()=>add('timelineEvents',{eventAt:v('timeline-at'),label:v('timeline-label'),description:v('timeline-description')},['timeline-at','timeline-label','timeline-description'],renderTimeline,x=>x.eventAt&&x.label);
}
function add(bucket,obj,clear,rer,validator=(x)=>Object.values(x).some(Boolean)){if(!validator(obj))return;state[bucket].push({id:id(),...obj});save();clear.forEach(i=>document.getElementById(i).value='');rer();renderStats();}
function row(label,type,x){return `<div class="item-header"><strong>${esc(label)}</strong><span><button data-a="up" data-t="${type}" data-id="${x.id}">↑</button><button data-a="down" data-t="${type}" data-id="${x.id}">↓</button><button data-a="edit" data-t="${type}" data-id="${x.id}">編集</button><button data-a="del" data-t="${type}" data-id="${x.id}" class="danger">削除</button></span></div>`;}
function bindList(type,arr,rer){document.querySelectorAll(`button[data-t="${type}"]`).forEach(b=>b.onclick=()=>{const i=arr.findIndex(x=>x.id===b.dataset.id);if(i<0)return;const a=b.dataset.a;if(a==='del')arr.splice(i,1);if(a==='up'&&i>0)[arr[i-1],arr[i]]=[arr[i],arr[i-1]];if(a==='down'&&i<arr.length-1)[arr[i+1],arr[i]]=[arr[i],arr[i+1]];if(a==='edit'){const next=prompt('JSON編集',JSON.stringify(arr[i],null,2));if(!next)return;try{arr[i]={...arr[i],...JSON.parse(next)}}catch{return alert('JSON形式が不正です')}}save();rer();if(type==='chapter')renderSceneChapterSelect();renderStats();});}
function renderChapters(){const el=document.getElementById('chapter-list');el.innerHTML=state.chapters.map(c=>`<li>${row(c.title,'chapter',c)}</li>`).join('');bindList('chapter',state.chapters,renderChapters);} 
function renderSceneChapterSelect(){const s=document.getElementById('scene-chapter');s.innerHTML=state.chapters.map(c=>`<option value="${c.id}">${esc(c.title)}</option>`).join('')||'<option value="">章なし</option>';}
function chapterName(id){return state.chapters.find(c=>c.id===id)?.title||'未所属';}
function renderScenes(){const el=document.getElementById('scene-list');el.innerHTML=state.scenes.map(s=>`<li>${row(`${s.title} (${chapterName(s.chapterId)})`,'scene',s)}<div class="muted">${esc((s.content||'').slice(0,120))}</div></li>`).join('');bindList('scene',state.scenes,renderScenes);} 
function renderCharacters(){const el=document.getElementById('character-list');el.innerHTML=state.characters.map(x=>`<li>${row(x.name,'character',x)}<div class="muted">${esc(x.role||'')} ${esc(x.note||'')}</div></li>`).join('');bindList('character',state.characters,renderCharacters);} 
function renderWorld(){const el=document.getElementById('world-list');el.innerHTML=state.worldEntries.map(x=>`<li>${row(`[${x.type}] ${x.name}`,'world',x)}<div class="muted">${esc(x.description||'')}</div></li>`).join('');bindList('world',state.worldEntries,renderWorld);} 
function renderTimeline(){const el=document.getElementById('timeline-list');el.innerHTML=state.timelineEvents.map(x=>`<li>${row(`${x.eventAt} ${x.label}`,'timeline',x)}<div class="muted">${esc(x.description||'')}</div></li>`).join('');bindList('timeline',state.timelineEvents,renderTimeline);} 
function renderStats(){const el=document.getElementById('stats');const items=[['章',state.chapters.length],['シーン',state.scenes.length],['文字数',state.scenes.reduce((n,s)=>n+(s.content||'').length,0)],['キャラ',state.characters.length],['世界観',state.worldEntries.length]];el.innerHTML=items.map(([k,v])=>`<div class="stat"><div class="muted">${k}</div><strong>${v}</strong></div>`).join('');}
async function ai(path,payload){const r=await fetch(path,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload)});return r.json();}
function bindAI(){document.getElementById('ai-plot').onclick=async()=>out(await ai('/api/ai/plot',{summary:v('ai-input')}));document.getElementById('ai-rewrite').onclick=async()=>out(await ai('/api/ai/rewrite',{text:v('ai-input')}));document.getElementById('ai-critique').onclick=async()=>out(await ai('/api/ai/critique',{text:v('ai-input')}));}
function out(x){document.getElementById('ai-output').textContent=JSON.stringify(x,null,2);}
function bindData(){document.getElementById('export-json').onclick=()=>{const b=new Blob([JSON.stringify(state,null,2)],{type:'application/json'});const a=document.createElement('a');a.href=URL.createObjectURL(b);a.download='note-architect-backup.json';a.click();URL.revokeObjectURL(a.href);};document.getElementById('import-json').onchange=async(e)=>{const f=e.target.files?.[0];if(!f)return;try{Object.assign(state,base(),JSON.parse(await f.text()));save();renderAll();}catch{alert('JSON読み込みに失敗しました')}};document.getElementById('clear-all').onclick=()=>{if(!confirm('全削除しますか？'))return;Object.assign(state,base());save();renderAll();};}
function renderAll(){renderStats();renderChapters();renderSceneChapterSelect();renderScenes();renderCharacters();renderWorld();renderTimeline();}
